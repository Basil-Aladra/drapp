# 🚀 البدء السريع - قاعدة بيانات MediClinic

## 📋 الخطوات الأساسية (5 دقائق)

### الخطوة 1: الانتقال إلى مجلد الخادم
```bash
cd server
```

### الخطوة 2: تثبيت المكتبات
```bash
npm install
```

### الخطوة 3: إنشاء ملف البيئة
أنشئ ملف `.env` في مجلد `server` واكتب:

```env
PORT=3001
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-secret-key-in-production"
NODE_ENV=development
```

### الخطوة 4: إعداد قاعدة البيانات
```bash
npm run db:generate
npm run db:migrate
npm run db:seed
```

### الخطوة 5: تشغيل الخادم
```bash
npm run dev
```

✅ **تم!** الخادم يعمل الآن على: http://localhost:3001

---

## 🔑 بيانات الدخول الافتراضية

بعد تشغيل `npm run db:seed`:

**المدير:**
- Email: `admin@clinic.com`
- Password: `admin123`

**الطبيب:**
- Email: `sarah@clinic.com`
- Password: `doctor123`

---

## 🧪 اختبار الـ API

### تسجيل الدخول:
```bash
POST http://localhost:3001/api/auth/login
Content-Type: application/json

{
  "email": "admin@clinic.com",
  "password": "admin123"
}
```

### الحصول على المرضى:
```bash
GET http://localhost:3001/api/patients
Authorization: Bearer YOUR_JWT_TOKEN
```

---

## 📚 الملفات المهمة

- **`README.md`** - توثيق كامل بالإنجليزية
- **`INSTRUCTIONS_AR.md`** - تعليمات تفصيلية بالعربية
- **`SETUP_GUIDE_AR.md`** - دليل الإعداد الشامل بالعربية

---

## 💡 أوامر مفيدة

| الأمر | الوصف |
|------|-------|
| `npm run dev` | تشغيل الخادم في وضع التطوير |
| `npm start` | تشغيل الخادم في وضع الإنتاج |
| `npm run db:studio` | فتح واجهة قاعدة البيانات |
| `npm run db:migrate` | تطبيق التغييرات على قاعدة البيانات |
| `npm run db:seed` | إضافة بيانات تجريبية |

---

## ❓ مشاكل شائعة؟

**المشكلة**: `Cannot find module '@prisma/client'`  
**الحل**: نفذ `npm install` ثم `npm run db:generate`

**المشكلة**: `Port 3001 already in use`  
**الحل**: غيّر `PORT` في ملف `.env`

**المشكلة**: `Migration failed`  
**الحل**: احذف ملف `dev.db` ثم نفذ `npm run db:migrate` مرة أخرى

---

## 🎯 الخطوة التالية

بعد تشغيل الخادم بنجاح، يمكنك:
1. ربط التطبيق الأمامي (React) مع هذا الخادم
2. استبدال البيانات المحلية بـ API calls
3. اختبار جميع الـ endpoints

---

**للمزيد من التفاصيل، راجع `SETUP_GUIDE_AR.md`**

