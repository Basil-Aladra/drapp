# دليل الإعداد الكامل - قاعدة بيانات MediClinic

## ✅ ما تم إنشاؤه

تم إنشاء نظام قاعدة بيانات كامل لمشروع MediClinic باستخدام Node.js و Express.js و Prisma ORM.

### 📁 هيكل المشروع

```
server/
├── prisma/
│   └── schema.prisma          # تعريف قاعدة البيانات والجداول
├── src/
│   ├── routes/                # مسارات API
│   │   ├── auth.routes.js     # تسجيل الدخول والتسجيل
│   │   ├── user.routes.js     # إدارة الأطباء
│   │   ├── patient.routes.js  # إدارة المرضى
│   │   ├── visit.routes.js    # إدارة الزيارات
│   │   ├── medication.routes.js # إدارة الأدوية
│   │   └── dashboard.routes.js  # إحصائيات Dashboard
│   ├── middleware/
│   │   └── auth.middleware.js # المصادقة والتفويض
│   ├── utils/
│   │   └── prisma.js          # اتصال Prisma
│   ├── seed.js                # إضافة بيانات تجريبية
│   └── server.js              # ملف الخادم الرئيسي
├── package.json               # التبعيات والمكتبات
├── README.md                  # توثيق بالإنجليزية
├── INSTRUCTIONS_AR.md         # تعليمات بالعربية
└── .gitignore                 # ملفات مستثناة من Git
```

### 🗄️ قاعدة البيانات

تم إنشاء قاعدة بيانات SQLite تحتوي على الجداول التالية:

1. **Users (المستخدمين)**
   - المديرين والأطباء
   - معلومات الأطباء: التخصص، الرواتب، التحولات

2. **Patients (المرضى)**
   - معلومات المرضى الأساسية
   - التاريخ الطبي: فصيلة الدم، الحساسية، الأمراض المزمنة
   - الديون المستحقة

3. **Visits (الزيارات)**
   - معلومات الزيارة: التاريخ، الطبيب، المريض
   - الفحوصات الحيوية: الوزن، الحرارة، الضغط، إلخ
   - التشخيص والملاحظات

4. **Medications (الأدوية)**
   - معلومات الأدوية المتاحة
   - المخزون والكميات

5. **PrescriptionMedications (وصفات الأدوية)**
   - الأدوية الموصوفة في كل زيارة
   - الجرعة والتعليمات

6. **TestOrders (الفحوصات)**
   - الفحوصات المطلوبة في كل زيارة
   - النتائج

### 🔌 API Endpoints

#### المصادقة (Authentication)
- `POST /api/auth/register` - تسجيل طبيب جديد (للمدير فقط)
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/me` - معلومات المستخدم الحالي

#### المستخدمين/الأطباء
- `GET /api/users/doctors` - جميع الأطباء
- `GET /api/users/doctors/:id` - طبيب محدد
- `PUT /api/users/doctors/:id` - تحديث معلومات طبيب
- `PUT /api/users/doctors/:id/shift-rates` - تحديث أسعار التحولات
- `PUT /api/users/doctors/:id/shift-counts` - تحديث عدد التحولات
- `POST /api/users/doctors/:id/reset-shifts` - إعادة تعيين التحولات
- `DELETE /api/users/doctors/:id` - حذف طبيب

#### المرضى
- `GET /api/patients` - جميع المرضى
- `GET /api/patients/:id` - مريض محدد
- `POST /api/patients` - إضافة مريض جديد
- `PUT /api/patients/:id` - تحديث معلومات مريض
- `DELETE /api/patients/:id` - حذف مريض
- `GET /api/patients/:id/visits` - زيارات مريض محدد

#### الزيارات
- `GET /api/visits` - جميع الزيارات
- `GET /api/visits/:id` - زيارة محددة
- `POST /api/visits` - إضافة زيارة جديدة
- `PUT /api/visits/:id` - تحديث زيارة
- `DELETE /api/visits/:id` - حذف زيارة

#### الأدوية
- `GET /api/medications` - جميع الأدوية
- `GET /api/medications/:id` - دواء محدد
- `POST /api/medications` - إضافة دواء جديد
- `PUT /api/medications/:id` - تحديث دواء
- `DELETE /api/medications/:id` - حذف دواء

#### الإحصائيات
- `GET /api/dashboard/stats` - إحصائيات Dashboard

## 🚀 الخطوات السريعة للبدء

### 1. الانتقال إلى مجلد الخادم
```bash
cd server
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إنشاء ملف .env
أنشئ ملف `.env` في مجلد `server` مع المحتوى التالي:

```env
PORT=3001
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
NODE_ENV=development
```

### 4. إعداد قاعدة البيانات
```bash
npm run db:generate
npm run db:migrate
npm run db:seed
```

### 5. تشغيل الخادم
```bash
npm run dev
```

الخادم سيعمل على: **http://localhost:3001**

## 🔐 الأمان

- جميع كلمات المرور مشفرة باستخدام bcrypt
- نظام مصادقة JWT للوصول إلى API
- تفويض حسب الأدوار (Admin/Doctor)
- معظم endpoints محمية وتتطلب تسجيل الدخول

## 📊 المميزات

✅ نظام مصادقة كامل مع JWT  
✅ إدارة المستخدمين والأطباء  
✅ إدارة المرضى والزيارات  
✅ تتبع الديون المستحقة تلقائياً  
✅ حساب رواتب الأطباء بناءً على التحولات  
✅ نظام وصفات طبية كامل  
✅ إدارة الفحوصات المطلوبة  
✅ إحصائيات Dashboard شاملة  
✅ قاعدة بيانات SQLite (سهلة الترقية لـ PostgreSQL/MySQL)  

## 🔄 الخطوات التالية

1. **ربط Frontend مع Backend**: تحتاج إلى تحديث ملفات React في `src/contexts/` للاتصال بـ API بدلاً من البيانات المحلية
2. **إضافة CORS**: تم إعداد CORS بالفعل
3. **إضافة Validation**: تم استخدام express-validator للتحقق من البيانات
4. **الترقية**: يمكن ترقية قاعدة البيانات لـ PostgreSQL/MySQL عند الحاجة

## 📝 ملاحظات مهمة

- قاعدة البيانات SQLite سهلة الاستخدام ولا تحتاج إعدادات معقدة
- ملف قاعدة البيانات (`dev.db`) سيُنشأ تلقائياً بعد تشغيل migrations
- البيانات التجريبية موجودة في ملف `seed.js`
- يمكن عرض قاعدة البيانات باستخدام Prisma Studio: `npm run db:studio`

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. تأكد من تثبيت جميع المكتبات: `npm install`
2. تأكد من وجود ملف `.env` بالقيم الصحيحة
3. تأكد من تشغيل `npm run db:generate` و `npm run db:migrate`
4. راجع ملف `README.md` للمزيد من التفاصيل

---

**تم إنشاء قاعدة البيانات بنجاح! 🎉**

